{
  "name": "group-policy-manager",
  "version": "1.0.0",
  "description": "Group Policy Manager - Electron app for managing Windows Group Policy settings",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "dev": "electron . --dev",
    "build": "set CSC_IDENTITY_AUTO_DISCOVERY=false && electron-builder",
    "build:win": "set CSC_IDENTITY_AUTO_DISCOVERY=false && electron-builder --win",
    "build:dir": "set CSC_IDENTITY_AUTO_DISCOVERY=false && electron-builder --dir",
    "pack": "set CSC_IDENTITY_AUTO_DISCOVERY=false && electron-builder --dir"
  },
  "keywords": [
    "electron",
    "group-policy",
    "windows",
    "security"
  ],
  "author": "Group Policy Manager Team",
  "license": "MIT",
  "dependencies": {
    "bcrypt": "^5.1.1"
  },
  "devDependencies": {
    "electron": "^28.0.0",
    "electron-builder": "^24.9.1"
  },
  "build": {
    "appId": "com.grouppolicymanager.app",
    "productName": "Group Policy Manager",
    "directories": {
      "output": "dist",
      "buildResources": "build"
    },
    "files": [
      "main.js",
      "preload.js",
      "renderer/**/*",
      "src/**/*",
      "package.json"
    ],
    "win": {
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        }
      ],
      "requestedExecutionLevel": "requireAdministrator",
      "artifactName": "${productName}-${version}-Setup.${ext}",
      "sign": null,
      "verifyUpdateCodeSignature": false
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "Group Policy Manager",
      "license": "LICENSE.txt"
    },
    "publish": null
  }
}
